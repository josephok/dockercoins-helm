apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dockercoins-all-service.fullname" . }}-test-connection"
  labels:
{{ include "dockercoins-all-service.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dockercoins-all-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

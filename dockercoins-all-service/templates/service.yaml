apiVersion: v1
kind: Service
metadata:
  name: {{ include "dockercoins-all-service.fullname" . }}
  labels:
{{ include "dockercoins-all-service.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "dockercoins-all-service.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
